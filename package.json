{
  "name": "streamflow-tauri",
  "version": "1.1.0",
  "description": "StreamFlow - Twitch Stream Manager with Tauri Architecture",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "release": "npm run build && echo 'Build complete! Check src-tauri/target/release/bundle/ for installers'"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
